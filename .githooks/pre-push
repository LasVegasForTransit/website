#!/usr/bin/env sh
# Keep the organization's one local gate while preserving the LFS hand-off
# that a custom hooks path would otherwise replace.
set -eu

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"
pnpm check

if command -v git-lfs >/dev/null 2>&1; then
  git lfs pre-push "$@"
fi
